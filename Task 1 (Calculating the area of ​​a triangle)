# Запитуємо основу та висоту
base = float(input("Введіть довжину основи трикутника: "))
height = float(input("Введіть висоту трикутника: "))
# Запитуємо одиниці вимірювання

unit = input("Введіть одиницю вимірювання (см або м): ").strip().lower()

# Перевірка одиниць вимірювання
if unit not in ["см", "м"]:
    print("Некоректна одиниця вимірювання. Використовуйте 'см' або 'м'.")
else:
    # Перевірка на додатні числа
    if base <= 0 or height <= 0:
        print("Основа та висота мають бути додатними числами.")
    else:
        # Обчислення площі
        area_trnl = 0.5 * base * height

        # Форматування результату (без зайвих нулів)
        formatted_area = int(area_trnl) if area_trnl.is_integer() else area_trnl

        # Вивід результату з одиницями
        print(f"Площа трикутника: {formatted_area} {unit}²")
